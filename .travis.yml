sudo: required
services:
    - docker

script:
    - docker build -t mannydesigns/node .
    - docker images mannydesigns/node

before_deploy:
    - docker login -u surabhi0404 -p Surabhig9@

deploy:
    provider: script
    script: docker push mannaydesigns/node
    on:
        branch: master
        